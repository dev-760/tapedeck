# TapeDeck Environment Variables
# Copy to .env and configure as needed

# Base URL for OctoFiesta music provider (Optional)
# If not set, app defaults to internal fallback or user settings
# EXPO_PUBLIC_OCTOFIESTA_URL=https://api.octofiesta.local

# Disable verbose logging in production
# EXPO_PUBLIC_DISABLE_LOGS=false
